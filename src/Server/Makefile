CPP_SRC   	= $(sort $(wildcard *.cpp)) # get src list
OBJ		  	= $(patsubst %.cpp,%.o,$(CPP_SRC)) # substitute to object list

all:$(OBJ)	# target
	
%.o:%.cpp	# dependencies and commands
	${GCC} ${CFLAG} -c $< ${STD}
clean:	# remove .o files generated by compilation
	$(shell rm *.o 2>/dev/null)